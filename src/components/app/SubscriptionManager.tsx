// Subscription management is no longer needed since there are no recurring plans.
// All purchases are one-time payments (gem sessions and cultural maps).
export default function SubscriptionManager() {
  return null;
}
